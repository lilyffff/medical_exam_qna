# 에러 해결에 사용한 PowerShell 명령어

이 문서는 현재 대화에서 프로젝트 오류를 조사하고 수정 결과를 검증하기 위해 실제로 실행한 PowerShell 명령을 시간 순서대로 기록한 것입니다.

> 범위: 현재 대화에서 확인 가능한 명령만 포함합니다. 사용자가 대화 이전에 직접 실행한 명령이나 IDE 터미널 기록은 확인할 수 없으므로 포함하지 않았습니다. 파일 수정은 PowerShell이 아닌 `apply_patch` 도구로 수행했습니다.

## 1. 전체 `app.py` 확인

정답 확인 버튼의 이벤트 처리 구조와 해설 생성 호출 위치를 조사했습니다.

```powershell
Get-Content -Raw app.py
```

## 2. 채점·해설·생성 설정 및 테스트 코드 검색

`grade_answer`, `make_explanation`, Transformers 생성 설정과 테스트 코드 존재 여부를 프로젝트 전체에서 검색했습니다.

```powershell
rg -n "grade_answer|make_explanation|generation_config|max_length|pytest|unittest" . -g "!*.ipynb" -g "!__pycache__"
```

## 3. 변경 내용 확인 및 Python 구문 검사

수정 후 차이점과 Python 구문 오류를 확인하려고 실행했습니다.

```powershell
git diff -- app.py; python -m py_compile app.py
```

실행 결과, 현재 폴더가 Git 저장소로 인식되지 않아 `git diff`는 다음 오류를 출력했습니다.

```text
warning: Not a git repository. Use --no-index to compare two paths outside a working tree
```

뒤이어 실행된 `python -m py_compile app.py`는 성공했습니다.

## 4. 오답 채점 함수 직접 실행

오답을 선택했을 때 AI 해설 생성을 기다리지 않고 정답 번호와 해설 버튼을 즉시 반환하는지 확인했습니다.

```powershell
python -c "import app; row={'answer':'4','A':'a','B':'b','C':'c','D':'d','E':'e'}; print(app.grade_answer(row, '1번'))"
```

이 과정에서 Gradio 6.0의 `theme` 매개변수 위치에 관한 경고도 발견했습니다.

```text
UserWarning: The parameters have been moved from the Blocks constructor to the launch() method in Gradio 6.0: theme.
```

## 5. 최종 구문 및 채점 결과 자동 검증

Gradio 경고를 수정한 뒤 구문 검사와 오답 채점 반환값을 `assert`로 검증했습니다.

```powershell
python -m py_compile app.py; python -c "import app; row={'answer':'4','A':'a','B':'b','C':'c','D':'d','E':'e'}; result=app.grade_answer(row, '1번'); assert result[0]['visible'] and '4번' in result[0]['value']; assert result[1]['visible']; assert not result[2]['visible']; print('grade_answer check: OK')"
```

검증 결과는 다음과 같았습니다.

```text
grade_answer check: OK
```

## 6. RAG 검색 코드와 관련 구현 전체 조사

참고 근거가 어떻게 추출되는지 확인하기 위해 `local_rag.py` 전체 내용과 프로젝트 내 RAG 관련 코드를 검색했습니다.

```powershell
Get-Content -Raw local_rag.py; rg -n "search_medical_knowledge|format_sources|index|embedding|chunk|similar" . -g "*.py"
```

## 7. RAG 인덱스 생성 코드·원본 자료·인덱스 파일 확인

문서 분할 방식과 의료 지식 데이터 파일, 생성된 SQLite 인덱스의 존재 및 크기를 확인했습니다.

```powershell
Get-Content -Raw build_rag_index.py; Get-ChildItem medical_knowledge_data -Recurse -File | Select-Object -First 15 FullName,Length; if (Test-Path rag_index.sqlite3) { Get-Item rag_index.sqlite3 | Select-Object FullName,Length,LastWriteTime }
```

## 8. 예시 문제의 실제 검색어와 상위 근거 확인

예시 문항, 정답 보기 및 선택 보기를 합친 검색문으로 핵심 검색어와 검색 결과를 직접 출력했습니다.

```powershell
python -c "from local_rag import query_terms,search_medical_knowledge,format_sources; q='복강내출혈이 심한 외상 환자의 24시간 소변량이 250mL일 때 추정할 수 있는 것은? 신장혈류량 저하 피부 대사량 증가'; print('TERMS:', query_terms(q)); print(format_sources(search_medical_knowledge(q)))"
```

이 실행을 통해 단순 OR 키워드 검색 때문에 관련성이 낮은 약물 문서가 상위 결과에 포함될 수 있다는 문제를 확인했습니다.

## 9. 설명 문서 생성 결과와 UTF-8 한글 확인

`설명가능한ai.md`가 생성되었는지 확인하고 첫 8줄을 UTF-8로 읽어 한글 저장 상태를 검증했습니다.

```powershell
Get-Item '설명가능한ai.md' | Select-Object FullName,Length; Get-Content -Encoding UTF8 '설명가능한ai.md' | Select-Object -First 8
```

## 10. 예시 SQL 문자열과 실제 생성 코드의 행 번호 검색

예시 SQL 문자열이 코드에 그대로 존재하는지 확인하고, 실제 FTS 검색문 생성 위치를 찾으려고 실행했습니다.

```powershell
rg -n -F '"복강내출혈" OR "외상"' .; rg -n "fts_query|query_terms\(|WHERE documents_fts MATCH" local_rag.py; rg -n -F '"복강내출혈" OR "외상"' '설명가능한ai.md'
```

첫 검색에서 예시 문자열은 `설명가능한ai.md` 65행에서 발견되었습니다. 이어지는 검색 결과가 출력에서 명확히 확인되지 않아 다음 명령으로 다시 점검했습니다.

## 11. 실제 FTS 코드와 설명 문서의 정확한 행 번호 재확인

PowerShell의 `Select-String`을 사용해 검색어 추출, OR 검색문 생성, FTS 실행문 및 설명 문서 예시의 정확한 행 번호를 확인했습니다.

```powershell
Select-String -Path local_rag.py -Pattern 'terms = query_terms','fts_query =','WHERE documents_fts MATCH' | Select-Object LineNumber,Line; Select-String -Path '설명가능한ai.md' -Pattern '복강내출혈.*OR.*외상' | Select-Object LineNumber,Line
```

확인된 위치는 다음과 같았습니다.

```text
local_rag.py 61행: terms = query_terms(question)
local_rag.py 65행: fts_query = " OR ".join(...)
local_rag.py 75행: WHERE documents_fts MATCH ?
설명가능한ai.md 65행: "복강내출혈" OR "외상" OR "24시간"
```

## PowerShell이 아닌 파일 수정 작업

다음 변경은 셸 명령이 아니라 안전한 패치 적용 도구인 `apply_patch`를 사용했습니다.

1. `app.py`에서 채점과 AI 해설 생성을 분리했습니다.
2. Transformers 생성 설정을 하나의 `generation_config`로 정리했습니다.
3. Gradio 테마 설정을 `demo.launch()`로 이동했습니다.
4. `설명가능한ai.md`를 생성했습니다.
5. 현재 문서인 `에러해결파워쉘명렁어.md`를 생성했습니다.

## 12. PowerShell 실행 정책으로 스크립트가 차단될 때

Python 가상환경의 `Activate.ps1` 같은 로컬 PowerShell 스크립트가 실행 정책 때문에 차단될 때 다음 명령을 사용했습니다.

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

확인 질문이 나타나면 내용을 확인한 뒤 실행에 동의합니다. 이 설정은 현재 Windows 사용자에게 적용되며, 로컬에서 만든 스크립트는 실행할 수 있게 하고 인터넷에서 받은 스크립트에는 서명을 요구합니다.

설정 결과는 다음 명령으로 확인합니다.

```powershell
Get-ExecutionPolicy
```

범위별 설정을 모두 확인하려면 다음 명령을 사용합니다.

```powershell
Get-ExecutionPolicy -List
```

그다음 프로젝트의 Python 가상환경을 활성화할 수 있습니다.

```powershell
.\.venv\Scripts\Activate.ps1
```

현재 PC를 확인했을 때는 다음 정책이 적용되어 있었습니다.

```text
LocalMachine    RemoteSigned
```

따라서 현재는 일반적인 로컬 PowerShell 스크립트를 실행할 수 있는 상태입니다.

### 현재 PowerShell 창에서만 임시로 허용하는 방법

사용자 설정을 영구적으로 바꾸고 싶지 않다면 현재 PowerShell 프로세스에서만 적용할 수 있습니다.

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
```

PowerShell 창을 닫으면 이 임시 설정은 사라집니다.

### `Bypass` 사용 주의

설치 과정에서 다음과 같이 실행 정책을 일시적으로 우회한 기록도 있었습니다.

```powershell
powershell -ExecutionPolicy Bypass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

이 명령은 인터넷에서 스크립트를 내려받아 즉시 실행합니다. URL과 제공자를 신뢰할 수 있는지 확인한 경우에만 사용해야 하며, 현재 의료 시험 프로젝트를 일반적으로 실행할 때는 다시 실행할 필요가 없습니다.

## 업데이트 001. Git 저장소·노트북 이전 점검 명령

### Git 저장소가 아니라는 오류

현재 폴더에서 다음 오류를 확인했습니다.

```text
fatal: not a git repository (or any of the parent directories): .git
```

GitHub에 처음 push하기 전 다음 명령으로 `main` 저장소를 초기화합니다.

```powershell
Set-Location -LiteralPath "D:\medical_exam_qna"
git init -b main
git remote add origin https://github.com/lilyffff/medical_exam_qna.git
```

초기화하기 전에 `.gitignore` 내용을 확인하고, 초기화한 뒤에는 비밀 파일 제외 여부를 반드시 검사합니다.

```powershell
Get-Content -LiteralPath ".gitignore"
git check-ignore -v .env
git check-ignore -v rag_index.sqlite3
git check-ignore -v rag_embeddings.npy
git check-ignore -v medical_knowledge_data
git status --short --ignored
```

### 노트북에서 Google Drive 경로가 맞는지 확인

```powershell
Get-PSDrive -PSProvider FileSystem
python -c "from pathlib import Path; import os; from dotenv import load_dotenv; load_dotenv(); names=['MEDICAL_KNOWLEDGE_PATH','RAG_INDEX_PATH','RAG_EMBEDDING_PATH','RAG_EMBEDDING_IDS_PATH','RAG_EMBEDDING_META_PATH']; print([(name, Path(os.environ[name]).exists()) for name in names])"
```

각 항목이 `True`인지 확인합니다. `False`이면 `.env`의 Drive 문자와 폴더명을 수정합니다.

### 외부 테스트 터널 명령

앱을 실행한 PowerShell과 별도의 창에서 다음 명령을 실행합니다.

```powershell
cloudflared tunnel --url http://127.0.0.1:7860
```

명령을 찾을 수 없다는 오류가 나오면 Cloudflare 공식 Windows 다운로드 안내에 따라 `cloudflared`를 설치하고 새 PowerShell에서 `cloudflared --version`을 확인합니다. 임시 URL은 공개하지 않습니다.

### 문서 기록 규칙

앞으로 코드 업데이트 때 사용한 조사·검증·복구용 PowerShell 명령은 `업데이트 NNN. 제목` 아래에 실행 순서대로 기록합니다. 명령을 실행하지 않은 경우에는 `추가 PowerShell 명령 없음`이라고 기록합니다.

## 업데이트 002. 실제 사용 환경변수만 유지

Python 코드가 실제로 참조하는 환경변수를 찾기 위해 다음 명령을 사용했습니다.

```powershell
rg -n "os\.getenv\(|os\.environ\[" . -g "*.py" -g "!__pycache__/**"
```

`.env.example`의 기존 내용을 확인했습니다.

```powershell
Get-Content -LiteralPath ".env.example"
```

실제 `.env`와 대조할 때는 토큰 값을 출력하지 않고 환경변수 이름과 비밀이 아닌 설정만 확인했습니다. 변경 후에는 다음 명령으로 예제 파일을 다시 확인할 수 있습니다.

```powershell
Get-Content -LiteralPath ".env.example"
```

이번 업데이트는 SQLite나 Python 실행 오류를 수정한 작업이 아니므로 추가 복구 명령은 없습니다.

### 검증 명령의 PowerShell 따옴표 오류와 수정

처음에는 `python -c` 안의 정규식에 작은따옴표와 큰따옴표를 함께 넣어 자동 비교하려 했으나 PowerShell이 명령 문자열을 먼저 잘못 해석해 `ParserError`가 발생했습니다. 프로젝트 파일 오류가 아니라 셸 따옴표 충돌이었습니다.

복잡한 중첩 따옴표를 사용하지 않고 PowerShell에서 직접 목록을 비교하도록 다음 명령으로 바꿨습니다.

```powershell
$used = rg -o 'os\.getenv\("[A-Z_]+' app.py local_rag.py build_embedding_index.py | ForEach-Object { if ($_ -match '([A-Z_]+)$') { $Matches[1] } } | Sort-Object -Unique
$example = Get-Content -LiteralPath .env.example | Where-Object { $_ -match '^[A-Z_]+=' } | ForEach-Object { ($_ -split '=',2)[0] } | Sort-Object -Unique
Compare-Object -ReferenceObject $used -DifferenceObject $example
```

`Compare-Object`가 아무 차이도 출력하지 않아 코드가 읽는 환경변수와 `.env.example`의 변수 목록이 일치함을 확인했습니다.

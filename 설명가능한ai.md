# 의료 문제 참고 근거 추출 과정

## 질문

> 복강내출혈이 심한 외상 환자의 24시간 소변량이 250mL일 때 추정할 수 있는 것은? 이런 문제에 대한 참고 근거는 어떻게 추출하니? 추출 과정을 설명해줘.

현재 앱은 임베딩 기반 의미 검색이 아니라, SQLite FTS5를 이용한 키워드 검색으로 참고 근거를 추출합니다.

이 문제의 정답은 일반적으로 **3번 신장혈류량 저하**입니다. 심한 복강내출혈로 순환혈액량이 감소하면 신장 관류가 떨어지고, 하루 소변량 250mL의 핍뇨가 나타날 수 있기 때문입니다.

## 현재 참고 근거 추출 과정

### 1. 검색문 생성

`app.py`의 `make_explanation()`에서 다음 세 항목을 합칩니다.

```python
search_query = " ".join([
    문제 본문,
    정답 보기,
    사용자가 선택한 보기,
])
```

예를 들어 사용자가 4번을 골랐다면 대략 다음 검색문이 됩니다.

```text
복강내출혈이 심한 외상 환자의 24시간 소변량이 250mL일 때
추정할 수 있는 것은?
신장혈류량 저하
중심정맥압 증가
```

정답 보기와 사용자가 고른 오답을 함께 검색하는 이유는 해설에서 두 보기를 비교할 근거를 찾기 위해서입니다.

### 2. 검색어 추출

`local_rag.py`의 `query_terms()`가 한글·영문·숫자로 이루어진 2글자 이상의 문자열을 최대 12개 추출합니다.

이 문항에서는 다음과 비슷한 검색어가 만들어집니다.

```text
복강내출혈
심한
외상
24시간
소변량이
250mL일
추정할
신장혈류량
저하
중심정맥압
증가
```

`환자`, `것은`, `다음`, `가장` 같은 일반적인 시험 표현은 제외합니다.

다만 조사나 어미를 제거하는 형태소 분석은 하지 않습니다. 따라서 `소변량이`, `250mL일`처럼 조사가 붙은 형태 그대로 검색됩니다.

### 3. FTS 후보 검색

추출한 단어를 `OR`로 연결합니다.

```sql
"복강내출혈" OR "외상" OR "24시간"
OR "소변량이" OR "신장혈류량" OR "저하"
```

`local_rag.py`의 `search_medical_knowledge()`에서 로컬의 `rag_index.sqlite3`를 검색하고, FTS의 BM25 순위에 따라 최대 80개 후보를 가져옵니다.

즉, 모든 단어가 포함될 필요는 없고 하나만 포함돼도 후보가 될 수 있습니다.

### 4. 후보 재정렬

80개 후보를 다음 기준으로 다시 정렬합니다.

```python
점수 = 일치한 서로 다른 검색어 수 * 100 \
       + 검색어가 등장한 전체 횟수
```

검색어 종류가 많이 겹치는 문서를 우선하고, 동점에 가까우면 SQLite BM25 순위를 반영합니다. 최종적으로 상위 4개 문서 조각만 선택합니다.

### 5. 문서 조각 생성 방식

인덱스를 만들 때 `build_rag_index.py`가 원본 의료 JSON을 다음과 같이 나눕니다.

- 한 조각당 최대 약 1,100자
- 인접 조각 사이에 180자 중복
- 가능하면 문장 끝에서 분리
- 전문 QA 자료는 `질문 + 답변` 형태로 저장
- 일반 원천 자료는 `content` 내용을 저장

이렇게 만든 조각들이 `rag_index.sqlite3`에 들어 있습니다.

### 6. 생성 모델에 전달

선정된 상위 4개 조각은 다음 형식으로 정리됩니다.

```text
[근거 1: 전문 QA · 자료명]
문서 내용...
파일: `원본 파일 경로`

[근거 2: 원천 문서 · 자료명]
문서 내용...
파일: `원본 파일 경로`
```

그런 다음 문제, 보기, 정답 번호, 사용자가 선택한 번호와 함께 해설 모델에 전달됩니다. 모델에는 “검색 근거에 없는 의학적 사실을 추가하지 말라”는 지시도 포함됩니다.

## 현재 방식의 한계

이 문항으로 실제 검색해보면 적절하지 않은 약물 문서 등이 상위에 섞일 가능성이 큽니다. 주요 원인은 다음과 같습니다.

- `외상`, `24시간`, `저하`, `증가`처럼 넓은 단어도 OR 조건으로 검색됨
- `복강내출혈 → 출혈성 쇼크 → 신장 관류 감소 → 핍뇨`라는 의미 관계를 이해하지 못함
- 한국어 조사를 제거하지 않아 `소변량`과 `소변량이`를 다른 표현처럼 취급할 수 있음
- 정답과 선택한 오답이 모두 검색문에 포함되어 검색 주제가 흐려질 수 있음
- `&#x20;`이 실제 데이터에 그대로 저장돼 있다면 `x20`이 불필요한 검색어로 추출될 수 있음

따라서 현재의 “참고 근거”는 정답을 입증한 문헌이라기보다 **문항과 단어가 많이 겹친 로컬 문서 조각**에 가깝습니다. 이 문제에는 `복강내출혈`, `출혈성 쇼크`, `핍뇨`, `신장 관류`, `신장혈류량 감소` 같은 개념으로 검색어를 확장하고, 임베딩 검색이나 형태소 분석을 추가하는 편이 훨씬 정확합니다.

---

# 현재 방식의 한계를 개선한 구현 기록

## 1. 개선 목표

특정 문항에만 검색어를 하드코딩하지 않고 의사·간호사·약사·치과의사 시험문제에 공통 적용되는 검색 흐름을 구현했습니다.

```text
문제 + 정답 보기
  → HTML·유니코드·단위 정규화
  → Kiwi 한국어 형태소 분석
  → 재사용 가능한 의료 개념 확장
  → SQLite FTS 키워드 검색
  → multilingual-E5 의미 검색
  → 두 순위 결합
  → 중복 원본 제거
  → 선정 방식과 점수가 포함된 근거 4개
```

## 2. 변경 전 기준선

다음 검색문으로 기존 결과를 기록했습니다.

```text
복강내출혈이 심한 외상 환자의 24시간 소변량이 250mL일 때
추정할 수 있는 것은? 신장혈류량 저하
```

기존 추출어는 다음과 같았습니다.

```text
복강내출혈이, 심한, 외상, 24시간, 소변량이,
250ml일, 추정할, 신장혈류량, 저하
```

상위 결과에는 다음과 같은 관련성 낮은 자료가 포함되었습니다.

1. 소아 정맥영양 수액 용량 문서
2. 외상 후 스트레스 장애 문서
3. 두부 외상과 인지기능 문서
4. 각막 외상 문서

숫자 `24·250`, 단위 `mL`, 일반어 `외상`이 순위를 지배하고 `소변량·신장 관류`의 병태생리 관계는 인식하지 못한 것이 원인이었습니다.

## 3. 텍스트 정규화

`local_rag.py`의 `normalize_medical_text()`를 추가했습니다.

처리 내용은 다음과 같습니다.

- `&#x20;` 같은 HTML 엔티티를 실제 문자로 변환
- Unicode NFKC 정규화
- 영문 대소문자 통일
- 연속 공백 제거
- `250 mL`와 `250mL` 같은 단위 표기 통일

예시는 다음과 같습니다.

```text
입력: &#x20; 24시간 소변량이 250 mL일 때
출력: 24시간 소변량이 250 ml일 때
```

## 4. Kiwi 한국어 형태소 분석

`kiwipiepy`를 사용해 조사와 어미를 제거하고 내용어 중심으로 검색합니다.

```text
복강내출혈이 → 복강, 출혈, 복강내출혈
소변량이     → 소변, 소변량
신장혈류량   → 신장, 혈류, 신장혈류량
```

숫자만으로 된 토큰과 `mL·mg·시간` 같은 단위는 의미 검색 문장에는 남기지만 FTS 핵심어에서는 제외합니다. Kiwi를 설치하지 못하거나 특정 입력 분석에 실패하면 정규식 검색으로 자동 대체합니다.

## 5. 의료 개념 확장

의료 개념 사전은 `medical_concepts.json`으로 소스코드에서 분리했습니다. 새로운 질환·동의어·병태생리 관계를 JSON에 추가할 수 있습니다.

출혈성 쇼크 그룹은 단서가 두 개 이상 발견될 때만 다음 개념을 확장합니다.

```text
입력 단서:
복강내출혈, 대량출혈, 출혈, 외상, 소변량, 핍뇨, 신장혈류

확장 개념:
출혈성 쇼크, 저혈량성 쇼크, 순환혈액량 감소,
장기 관류 저하, 신장 관류 감소, 신장혈류량 감소, 핍뇨
```

`출혈` 하나만 등장하는 모든 문제를 쇼크로 오해하지 않도록 `min_matches=2` 조건을 사용합니다. 현재 사전에는 아나필락시스, 자간전증, 감염성 폐결핵 격리 그룹도 포함했습니다.

## 6. 검색문에서 사용자의 오답 분리

변경 전에는 다음을 모두 하나의 검색문으로 사용했습니다.

```text
문제 + 정답 보기 + 사용자가 선택한 오답
```

이제 정답 근거 검색에는 다음만 사용합니다.

```text
문제 + 정답 보기
```

사용자가 선택한 오답은 검색을 왜곡하지 않도록 검색문에서 제외하고, MediKo의 오답 분석 프롬프트에서만 사용합니다.

## 7. SQLite FTS 검색 개선

기존 SQLite FTS5 검색은 계속 유지합니다. 정확한 질환명·약물명·검사명·의학 약어에는 키워드 검색이 강하기 때문입니다.

개선 사항은 다음과 같습니다.

1. 형태소 원형과 복합 의학용어를 함께 검색
2. 긴 희귀 의학용어 8개는 개별 검색해 OR 검색에서 밀리지 않게 함
3. 후보를 최대 100개 확보
4. 일치 단어 개수뿐 아니라 단어 길이의 제곱을 반영해 구체적인 용어를 우선

예를 들어 `자간전증`은 `혈압`보다 더 구체적이므로 높은 키워드 가중치를 받습니다.

## 8. multilingual-E5 의미 검색

모델은 다음을 사용합니다.

```text
intfloat/multilingual-e5-small
```

문서는 `passage:` 접두사, 질문은 `query:` 접두사를 붙이고 attention mask 평균 풀링과 L2 정규화를 적용합니다.

Windows 애플리케이션 제어 정책이 Sentence Transformers가 설치한 SciPy DLL을 차단했기 때문에, 차단된 패키지는 제거하고 기존 `transformers + torch`만 사용해 E5 모델 카드와 같은 임베딩 계산을 직접 구현했습니다. 관련 코드는 `embedding_model.py`에 있습니다.

## 9. 의미 검색 대상 자료 선정

전체 SQLite 문서 조각은 99,742개지만 일반 온라인 의료정보가 77,234개로 대부분을 차지하고 약품 설명 노이즈가 많았습니다.

의미 인덱스는 다음 고품질·전문 자료 22,508개로 구성했습니다.

```text
전문 QA
학회 가이드라인
의학 교과서
학술 논문 및 저널
기타 의료 원천 문서
```

일반 온라인 의료정보는 의미 인덱스에서 제외하지만 전체 SQLite FTS 검색에서는 계속 사용할 수 있습니다.

## 10. 임베딩 인덱스 생성

다음 명령을 한 번 실행했습니다.

```powershell
python build_embedding_index.py
```

CPU 처리 시간을 줄이기 위해 문서당 최대 256토큰, 배치 크기 64를 사용했습니다. 인덱스는 먼저 `.building` 임시 파일에 기록하고 전체 성공 후 정식 파일로 교체합니다. 중간 실패 시 앱이 불완전한 파일을 읽지 않습니다.

생성 결과는 다음과 같습니다.

```text
rag_embeddings.npy       22,508 × 384 float32 벡터, 약 34.6MB
rag_embedding_ids.npy    SQLite 문서 ID 연결, 약 180KB
rag_embeddings.meta.json 모델·문서 수·설정 정보
```

## 11. 하이브리드 순위 결합

BM25와 코사인 유사도는 점수 범위가 서로 달라 직접 더하기 어렵습니다. 따라서 각 검색의 순위를 Reciprocal Rank Fusion으로 결합합니다.

```text
키워드 순위 점수 = 1.0 / (60 + 순위)
의미 검색 점수   = 1.2 / (60 + 순위)
최종 점수        = 두 점수의 합
```

의미 검색에 1.2배를 주어 숫자나 일반 단어가 많은 문서가 앞서는 현상을 줄이면서, 정확한 전문용어가 양쪽 검색에 잡히면 두 점수를 모두 받아 우선하도록 했습니다.

같은 `source_path`의 겹치는 조각은 하나만 선택해 근거 네 칸을 동일 원본이 차지하지 않게 했습니다.

## 12. 설명 가능한 검색 결과

각 근거에는 다음 정보가 함께 표시됩니다.

```text
선정 방식: 의미 임베딩 + 형태소·키워드
일치 개념: 출혈, 외상, 소변, 신장, 혈류, 저하
의미 유사도: 0.900
확장 의료 개념: 저혈량성·출혈성 쇼크와 장기 저관류
원본 파일: ...
```

따라서 문서가 어떤 검색 경로와 개념 때문에 선택됐는지 사용자가 확인할 수 있습니다.

## 13. 장애 시 자동 대체

다음 상황에서도 앱이 중단되지 않도록 했습니다.

- 임베딩 `.npy` 파일 없음
- 메타데이터와 모델 이름 불일치
- E5 모델 로딩 실패
- NumPy 또는 임베딩 계산 오류
- Kiwi 설치 또는 분석 실패

임베딩을 사용할 수 없으면 형태소 기반 SQLite FTS 검색으로 자동 전환합니다. 실제로 임베딩 경로를 존재하지 않는 파일로 설정한 테스트에서 근거 4개가 정상 반환되는 것을 확인했습니다.

## 14. 검색 품질 검증 결과

### 출혈성 쇼크와 핍뇨

변경 전에는 영양수액·PTSD·각막 외상 문서가 선택됐습니다. 변경 후에는 다음 두 종류의 근거가 함께 검색됩니다.

1. 신장 혈류 감소와 소변량 감소의 관계를 설명하는 전문 QA
2. 혈액량 감소성 쇼크와 지속적 저관류를 설명하는 원천 문서

한 문서에 전체 인과관계가 모두 없더라도 두 근거를 결합해 다음 연결을 구성할 수 있습니다.

```text
대량 출혈 → 저혈량성 쇼크 → 장기 저관류
장기 저관류 → 신장 혈류 감소 → 소변량 감소
```

### 아나필락시스

변경 전에는 일반 철분 근육주사 문서가 1위였습니다. 변경 후에는 다음이 상위에 배치됐습니다.

1. 아나필락시스 치료 약물인 아드레날린·에피네프린 전문 QA
2. 저혈압을 동반한 아나필락시스에서 에피네프린을 우선 투여하는 전문 QA

### 감염성 폐결핵

비말핵이 공기 중에서 장시간 감염력을 유지한다는 교과서 문서가 1위였고, 객담 결핵균 양성 환자의 초기 조치 QA가 뒤를 이었습니다.

### 자간전증

자간전증·임신성 고혈압 관련 자료를 검색했지만 현재 원천 데이터는 직접 진단 기준보다 예방과 합병증 설명이 많았습니다. 검색 기술만으로 원문에 없는 직접 근거를 만들 수 없으므로 이 분야는 관련 산과 가이드라인 또는 교과서 자료를 추가해야 더 좋아집니다.

## 15. 성능 측정

CPU 환경에서 결핵 검색으로 측정한 결과입니다.

```text
첫 검색: 약 8.18초
  - E5 모델을 메모리에 처음 로딩하는 시간 포함

두 번째 검색: 약 0.084초
  - 로딩된 모델과 메모리 매핑 인덱스 재사용
```

벡터 파일은 NumPy 메모리 매핑으로 열어 전체를 별도 RAM 복사하지 않습니다.

## 16. 다른 문제에 적용되는 방식

새로운 문제는 다음 세 층을 자동으로 사용합니다.

1. 모든 한국어 문제: 정규화와 Kiwi 형태소 분석
2. 사전에 없는 개념: multilingual-E5 의미 검색
3. 반복적으로 중요한 병태생리·동의어: `medical_concepts.json` 확장

새 개념은 Python 코드를 수정하지 않고 JSON에 다음 구조로 추가할 수 있습니다.

```json
{
  "concept": "개념 이름",
  "min_matches": 1,
  "triggers": ["문제에서 발견할 표현"],
  "expansions": ["동의어", "병태생리", "관련 임상 표현"]
}
```

너무 일반적인 단어를 trigger로 사용할 때는 `min_matches`를 2 이상으로 설정해 잘못된 확장을 줄여야 합니다.

## 17. 남아 있는 한계

개선 후에도 검색 결과가 정답을 법적으로나 학술적으로 확정하는 원문 인용이라는 뜻은 아닙니다.

- 로컬 데이터에 직접 근거가 없으면 유사한 다단계 근거만 찾을 수 있음
- multilingual-E5는 일반 다국어 모델이며 한국 의료시험 전용으로 학습된 모델은 아님
- 의료 개념 사전은 계속 검토하고 확장해야 함
- 의미 유사도가 높아도 의학적 인과관계가 정확하다는 보장은 없음
- 실제 검색 품질은 직종별 평가 문제 세트로 지속 측정해야 함

따라서 생성 모델에는 계속 “검색 근거에 없는 사실을 추가하지 말라”는 제한을 적용하고, 실제 진료가 아닌 시험 학습용이라는 안전 고지를 유지합니다.

## 18. 3단계 설명 안전 가드레일

검색 결과가 좋아져도 생성 모델이 근거에 없는 사실을 덧붙일 가능성을 완전히 없앨 수는 없습니다. 이를 줄이기 위해 `프롬프트 제한 → 검색 품질 검사 → 생성 결과 검사`의 세 단계를 적용했습니다.

### 18.1 프롬프트 제한

생성 모델에는 다음 규칙을 명시합니다.

1. `[검색 근거]` 안의 내용만 사용합니다.
2. 모델이 미리 알고 있던 사실이나 추측으로 부족한 내용을 채우지 않습니다.
3. 가능한 경우 의학적 설명에 `[근거 1]` 형식으로 출처 번호를 표시합니다.
4. 검색 결과에 없는 근거 번호를 만들지 않습니다.
5. 실제 환자의 진단·치료 지시가 아닌 시험 학습용 해설만 작성합니다.

프롬프트는 첫 번째 방어선입니다. 모델이 지시를 항상 완벽하게 따른다고 보장할 수 없으므로 아래의 코드 검사도 함께 사용합니다.

### 18.2 검색 품질 검사

검색 결과가 존재한다는 이유만으로 바로 해설을 생성하지 않습니다. `assess_evidence_quality()`가 다음 조건을 검사합니다.

1. 정답 보기에서 추출한 핵심 의학용어가 검색 문서에 실제로 존재하는지 확인합니다.
2. 한 문서에서 문제·정답과 관련된 핵심어가 두 개 이상 일치하는지 확인합니다.
3. 핵심어가 한 개만 일치한다면 의미 임베딩 유사도가 기본값 `0.86` 이상인지 확인합니다.
4. 위 조건을 충족하지 않으면 모델을 실행하지 않고 `가드레일: 해설 생성 중단`을 표시합니다.

임계값은 `.env`에서 다음과 같이 조정할 수 있습니다.

```dotenv
RAG_GUARDRAIL_MIN_SEMANTIC=0.86
```

값을 높이면 더 엄격해지지만 유효한 근거도 차단할 수 있고, 값을 낮추면 관련성이 약한 근거가 통과할 수 있습니다. 따라서 직종별 평가 문제로 검증한 뒤 조정해야 합니다.

### 18.3 생성 결과 검사

모델이 해설을 만든 뒤 `validate_generated_explanation()`이 화면에 표시하기 전에 다시 검사합니다.

- 지나치게 짧거나 같은 구절을 반복하는 출력
- 데이터셋의 정답 번호가 포함되지 않은 출력
- 개인 사용자를 대상으로 한 진단·투약·응급 지시
- 검색 결과에 존재하지 않는 `[근거 N]` 인용
- 검색 근거와 핵심 의학 개념이 거의 겹치지 않는 출력
- 문제, 보기, 검색 근거에 없던 용량·기간·비율 등의 의학 수치

하나라도 발견되면 생성문을 숨기고 정답, 차단 사유, 검색된 원문 근거만 표시합니다. 따라서 사용자는 모델이 만든 부정확한 설명 대신 실제 검색 자료를 직접 검토할 수 있습니다.

### 18.4 전체 처리 흐름

```text
문제 + 정답 보기로 근거 검색
        ↓
정답 핵심어·검색 일치도·의미 유사도 검사
        ├─ 부족 → 모델 실행 중단 + 검색 근거 표시
        ↓ 충분
근거 제한 프롬프트로 해설 생성
        ↓
정답 번호·근거 번호·수치·위험 지시·개념 연결 검사
        ├─ 실패 → 생성문 숨김 + 차단 사유 + 검색 근거 표시
        ↓ 통과
해설 + 검색 품질 결과 + 참고 근거 + 안전 고지 표시
```

### 18.5 검증

`test_guardrails.py`에 다음 회귀 테스트를 추가했습니다.

1. 정답 개념과 검색 핵심어가 충분한 근거는 통과
2. 정답과 무관한 문서는 높은 의미 유사도만으로 통과하지 못함
3. 근거와 연결된 정상적인 시험 해설은 통과
4. 존재하지 않는 근거 번호와 출처에 없는 수치는 차단

기존 하이브리드 검색 테스트 6개와 새 가드레일 테스트 4개가 모두 통과했습니다. 이 가드레일은 의료적 정확성을 보증하는 장치가 아니라, 근거가 부족할 때 모델이 자신 있게 내용을 만들어 내는 위험을 줄이는 장치입니다.

## 업데이트 001. 문서 동기화 및 노트북 이전 절차

현재 구현을 기준으로 `플로우챠트.md`의 초기 FTS 전용 흐름을 하이브리드 검색과 3단계 가드레일 흐름으로 보완했습니다. `노트북으로이사.md`에는 공개 GitHub에는 코드만 올리고, 비공개 Google Drive에는 토큰·의료 원문·SQLite·임베딩 파일을 보관하는 절차를 작성했습니다.

앞으로 코드 업데이트는 동일한 `업데이트 NNN. 제목`을 사용하여 다음 다섯 문서에 함께 기록합니다.

1. `설명가능한ai.md`: 변경 목적, 알고리즘, 한계와 검증
2. `에러해결파워쉘명렁어.md`: 조사·검증·복구 명령
3. `플로우챠트.md`: 실행 순서와 파일·함수 관계
4. `환경변수.md`: 추가·변경·폐기된 설정과 보안 주의
5. `sqllite사용설명서.md`: SQLite와 검색 인덱스에 미치는 영향

해당되지 않는 문서에도 기록을 생략하지 않고 `영향 없음`과 이유를 남깁니다. 실제 비밀 값은 어느 문서에도 기록하지 않습니다.

# 프로젝트 환경변수 정리

확인 기준: 2026-09-01, 프로젝트 경로 `D:\medical_exam_qna`

이 문서는 `.env`, `.env.example`, 현재 PowerShell 프로세스 환경 및 코드의 기본값을 대조한 결과입니다. 토큰, API 키 및 프록시 인증정보는 보안을 위해 실제 값을 기록하지 않습니다.

## 현재 앱에서 사용되는 환경변수

| 환경변수 | 현재 실효값 | 값의 출처 | 사용 목적 |
|---|---|---|---|
| `HF_TOKEN` | 설정됨(값 비공개) | `.env` | Hugging Face 데이터셋 및 모델 접근 인증 |
| `KORMED_DATASET_ID` | `sean0042/KorMedMCQA` | `app.py` 기본값 | 의료 국가시험 문제 데이터셋 지정 |
| `MEDICAL_BASE_MODEL` | `MediKo/MediKo-1.1B-A0.6B-inst` | `.env` | 해설 생성용 기본 언어 모델 |
| `MEDICAL_LORA_ADAPTER` | 미설정 | `.env`의 빈 값 | 선택적인 의료 LoRA 어댑터 경로 |
| `MEDICAL_TRUST_REMOTE_CODE` | `true` | `.env` | Hugging Face 모델 저장소의 사용자 정의 Python 코드 허용 |
| `MEDICAL_KNOWLEDGE_PATH` | `medical_knowledge_data` | `.env` | 로컬 전문 의학지식 JSON 폴더 경로 |
| `RAG_INDEX_PATH` | `rag_index.sqlite3` | `.env` | SQLite FTS5 검색 인덱스 경로 |
| `RAG_CONCEPT_PATH` | `medical_concepts.json` | `.env` | 재사용 가능한 의료 개념·동의어 확장 사전 |
| `RAG_EMBEDDING_MODEL` | `intfloat/multilingual-e5-small` | `.env` | 의미 검색 임베딩 모델 |
| `RAG_EMBEDDING_PATH` | `rag_embeddings.npy` | `.env` | 문서 의미 벡터 배열 |
| `RAG_EMBEDDING_IDS_PATH` | `rag_embedding_ids.npy` | `.env` | 벡터와 SQLite 문서 ID 연결 배열 |
| `RAG_EMBEDDING_META_PATH` | `rag_embeddings.meta.json` | `.env` | 임베딩 모델·문서 수·DB 크기 메타데이터 |
| `RAG_EMBEDDING_DEVICE` | `cpu` | `.env` | 임베딩 계산 장치 |
| `RAG_CANDIDATE_LIMIT` | `100` | `.env` | 키워드 및 의미 검색 후보 수 |
| `RAG_EMBEDDING_BATCH_SIZE` | `64` | `.env` | 인덱스 생성 배치 크기 |
| `RAG_EMBEDDING_MAX_LENGTH` | `256` | `.env` | 문서·질문 임베딩 최대 토큰 길이 |
| `APP_HOST` | `0.0.0.0` | `.env` | 같은 Wi-Fi 기기의 접속을 받는 Gradio 서버 주소 |
| `APP_PORT` | `7860` | `.env` | Gradio 웹 서버 포트 |

현재 작업 디렉터리에서 상대 경로를 절대 경로로 해석하면 다음과 같습니다.

```text
MEDICAL_KNOWLEDGE_PATH = D:\medical_exam_qna\medical_knowledge_data
RAG_INDEX_PATH         = D:\medical_exam_qna\rag_index.sqlite3
```

`APP_HOST=0.0.0.0`은 PC의 모든 네트워크 인터페이스에서 접속을 받습니다. 현재 앱은 `share=False`이므로 Gradio 외부 공유 링크는 만들지 않으며, 15단계에서는 같은 Wi-Fi 내부 접속에만 사용합니다.

## 추가로 정의된 환경변수

| 환경변수 | 현재 상태 | 설명 |
|---|---|---|
| `OPENAI_API_KEY` | 설정됨(값 비공개) | `.env`와 현재 프로세스 환경에서 확인되지만, 현재 Python 코드에서는 사용하지 않음 |
| `HTTPS_PROXY` | 미설정 | HTTPS 통신에 기관 프록시가 필요한 경우 사용 |
| `HTTP_PROXY` | 미설정 | HTTP 통신에 기관 프록시가 필요한 경우 사용 |

현재 PowerShell 프로세스에는 `OPENAI_API_KEY`가 이미 설정되어 있습니다. `python-dotenv`의 `load_dotenv()`는 기본적으로 기존 프로세스 환경변수를 덮어쓰지 않으므로, 앱 실행 시에는 프로세스에 설정된 `OPENAI_API_KEY`가 우선합니다. 다만 현재 앱 코드는 이 변수를 참조하지 않습니다.

## 코드에서의 로딩 순서

`app.py`는 실행 초기에 프로젝트의 `.env`를 읽습니다.

```python
load_dotenv(Path(__file__).with_name(".env"))
```

환경변수의 일반적인 우선순위는 다음과 같습니다.

1. 앱 실행 전 PowerShell 또는 운영체제 프로세스에 이미 설정된 값
2. 프로젝트의 `.env`에 작성된 값
3. 코드의 `os.getenv()`에 지정된 기본값

예를 들어 `KORMED_DATASET_ID`는 현재 `.env`에 없고 프로세스 환경에도 없으므로 다음 코드 기본값을 사용합니다.

```python
DATASET_ID = os.getenv("KORMED_DATASET_ID", "sean0042/KorMedMCQA")
```

## `.env.example`에 안내된 기본값

새로운 환경을 구성할 때 `.env.example`은 다음 값을 예시로 제공합니다.

```dotenv
HF_TOKEN=
OPENAI_API_KEY=
KORMED_DATASET_ID=sean0042/KorMedMCQA
MEDICAL_BASE_MODEL=Qwen/Qwen2.5-1.5B-Instruct
MEDICAL_LORA_ADAPTER=
MEDICAL_TRUST_REMOTE_CODE=false
MEDICAL_KNOWLEDGE_PATH=medical_knowledge_data
RAG_INDEX_PATH=rag_index.sqlite3
APP_HOST=0.0.0.0
APP_PORT=7860
```

실제 `.env`와 비교하면 다음 두 설정이 예시 기본값에서 변경되어 있습니다.

| 환경변수 | `.env.example` | 현재 `.env` |
|---|---|---|
| `MEDICAL_BASE_MODEL` | `Qwen/Qwen2.5-1.5B-Instruct` | `MediKo/MediKo-1.1B-A0.6B-inst` |
| `MEDICAL_TRUST_REMOTE_CODE` | `false` | `true` |

## 보안 주의사항

- `.env`에는 실제 토큰과 API 키가 있으므로 Git에 커밋하지 않아야 합니다.
- `HF_TOKEN`과 `OPENAI_API_KEY`의 실제 값은 문서, 로그 또는 화면 캡처에 기록하지 않아야 합니다.
- `MEDICAL_TRUST_REMOTE_CODE=true`는 모델 저장소의 사용자 정의 코드를 실행할 수 있게 합니다. 출처와 코드를 신뢰할 수 있는 모델에서만 사용해야 합니다.
- 프록시 URL에 계정이나 비밀번호가 포함되는 경우에도 토큰과 동일하게 비밀정보로 취급해야 합니다.

## 업데이트 001. 가드레일 및 노트북 Google Drive 경로

### 새 가드레일 환경변수

| 환경변수 | 기본값 | 설명 |
|---|---:|---|
| `RAG_GUARDRAIL_MIN_SEMANTIC` | `0.86` | 검색 핵심어가 1개만 일치할 때 근거로 인정할 최소 의미 유사도 |

값을 높이면 생성 전에 더 많은 검색 결과가 차단되고, 낮추면 관련성이 약한 문서가 통과할 수 있습니다. 직종별 평가 결과 없이 임의로 낮추지 않는 것이 좋습니다.

### 노트북에서의 권장 배치

노트북의 `.env`는 GitHub clone 폴더에 두되 `.gitignore`로 계속 제외합니다. 실제 토큰이 든 원본 `.env`, 의료 원문과 대용량 실행 파일은 비공개 Google Drive의 다음 폴더에 둡니다.

```text
medical_exam_qna/source/medical_knowledge_data
medical_exam_qna/runtime/.env
medical_exam_qna/runtime/rag_index.sqlite3
medical_exam_qna/runtime/rag_embeddings.npy
medical_exam_qna/runtime/rag_embedding_ids.npy
medical_exam_qna/runtime/rag_embeddings.meta.json
```

노트북 `.env`의 `MEDICAL_KNOWLEDGE_PATH`, `RAG_INDEX_PATH`, `RAG_EMBEDDING_PATH`, `RAG_EMBEDDING_IDS_PATH`, `RAG_EMBEDDING_META_PATH`에는 Drive for desktop에서 확인한 실제 경로를 지정합니다. 드라이브 문자는 PC마다 달라질 수 있으므로 문서에 고정된 값으로 가정하지 않습니다.

### 문서 기록 규칙

앞으로 코드 업데이트는 `업데이트 NNN. 제목` 형식으로 다섯 프로젝트 문서에 같은 번호로 기록합니다. 토큰, API 키, 터널 토큰과 인증정보의 실제 값은 기록하지 않습니다.
